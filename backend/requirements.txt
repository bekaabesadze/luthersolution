# Competitor Bank Analytics Backend - Python dependencies
# Install in a virtual environment: python -m venv venv && source venv/bin/activate && pip install -r requirements.txt

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
sqlalchemy>=2.0.0
pandas>=2.0.0
openpyxl>=3.1.0
arelle-release>=2.0.0
opencv-python-headless>=4.8.0
