# Loaded by Vite for `npm run dev` only. Use the hosted API so Outlook and other routes work
# without a matching local backend. Override with .env.development.local (gitignored), e.g.:
#   VITE_API_BASE_URL=http://127.0.0.1:8001
VITE_API_BASE_URL=https://luthersolution.onrender.com
